apply plugin: 'com.android.application'

android {
    compileSdkVersion 33

    defaultConfig {
        applicationId "com.example.your_project_name"
        minSdkVersion 21
        targetSdkVersion 33
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    implementation 'com.android.support:appcompat-v7:28.0.0'
    // إضافة الحزم الأخرى التي تحتاج إليها هنا
}

// في آخر الملف مباشرةً:
apply plugin: 'com.google.gms.google-services'  // تأكد من إضافته في هذا المكان
